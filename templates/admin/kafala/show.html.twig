{% extends 'base.html.twig' %}

{% block title %}Kafala{% endblock %}

{% block body %}
    <h1>Kafala</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kafala.id }}</td>
            </tr>
            <tr>
                <th>SetAtdebut</th>
                <td>{{ kafala.setAtdebut ? kafala.setAtdebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ kafala.type }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ kafala.prix }}</td>
            </tr>
            <tr>
                <th>Periode</th>
                <td>{{ kafala.periode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('kafala_index') }}">back to list</a>

    <a href="{{ path('kafala_edit', {'id': kafala.id}) }}">edit</a>

    {{ include('kafala/_delete_form.html.twig') }}
{% endblock %}
